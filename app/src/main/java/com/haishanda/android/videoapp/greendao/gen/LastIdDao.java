package com.haishanda.android.videoapp.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.haishanda.android.videoapp.Bean.LastId;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "LAST_ID".
*/
public class LastIdDao extends AbstractDao<LastId, Void> {

    public static final String TABLENAME = "LAST_ID";

    /**
     * Properties of entity LastId.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property LastId = new Property(0, int.class, "lastId", false, "LAST_ID");
    }


    public LastIdDao(DaoConfig config) {
        super(config);
    }
    
    public LastIdDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"LAST_ID\" (" + //
                "\"LAST_ID\" INTEGER NOT NULL UNIQUE );"); // 0: lastId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"LAST_ID\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, LastId entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getLastId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, LastId entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getLastId());
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public LastId readEntity(Cursor cursor, int offset) {
        LastId entity = new LastId( //
            cursor.getInt(offset + 0) // lastId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, LastId entity, int offset) {
        entity.setLastId(cursor.getInt(offset + 0));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(LastId entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(LastId entity) {
        return null;
    }

    @Override
    public boolean hasKey(LastId entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
